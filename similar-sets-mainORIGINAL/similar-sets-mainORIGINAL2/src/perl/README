
Perl scripts for preparing the datasets and checking exhaustively similarity of sets.


1) Sorting a dataset

$ ./sort-dataset.pl < dataset > dataset.sorted 

2) Computing frequencies of datatset elements

$ ./symb-freq.pl < dataset | sort -n -r -k2 > dataset.freq

3) Generate a mapping based on frequencies

$ ./gen-map-onfreq.pl < dataset.freq > dataset.freq.map

4) Map a dataset on the basis of element frequencies

$ ./map-onfreq-dataset.pl dataset.freq.map < dataset > dataset.mapd

5) Sorting a mapped dataset

$ ./sort-dataset.pl < dataset.mapd > dataset.mapd.sorted

6) Generate a testset from a dataset

$ ./gen-testset.pl percent < dataset.mapd > dataset.mapd.test

7) Sorting a testset

$ ./sort-dataset.pl < dataset.mapd.test > dataset.mapd.test.sorted 

8) Check exhaustively the similarity of sets from a testset against a dataset. 
   Each set from a testset is checked against every set from a dataset.
   The result should be the same as the result produced with set2 
   (minus timestamps in set2 result).

$ ./check-similar.pl distance dataset.mapd.sorted < dataset.mapd.test.sorted > dataset.mapd.test.sorted.result

9) Compute the average running time of queries obtained with set2.

$ ./time-average.pl < dataset.mapd.test.sorted.result


Experiments with src/C/set2 use dataset.mapd.sorted and dataset.mapd.test.sorted.
The program set2 can be generated by using Makefile.
See more in src/C/README.
